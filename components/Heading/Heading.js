import styled from "styled-components";

const Heading = styled.section`
  text-align: center;
  background-color: var(--rosehip-color);
`;

export default function Head() {
  return <Heading>Restaurants</Heading>;
}
